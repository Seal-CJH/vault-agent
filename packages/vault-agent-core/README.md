# Vault Agent Core

The local policy core for Vault Agent. It indexes a Markdown vault, compiles bounded context, performs explicitly confirmed model calls, validates Ingest Packets, and permits staged writes only under the configured Inbox policy.

This package does not contain API keys, provider settings, vault contents, or local session state. See the repository root for installation, client protocol, and release documentation.
